#include <stdio.h>
void main()
{
    int a;
    printf("enter the no:");
    scanf("%d",&a);
    if(a>0)

        printf("the no is positive");
        else if(a==0)
        printf("the no is zero");
    
    else
    
        printf("the no is negative");

}
